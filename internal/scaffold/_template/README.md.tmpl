# {{ .ProjectName }}

{{ .ProjectName }} is a Go CLI project generated by `go-cli-starter`.

## Quick Start

```bash
go mod tidy
go build -o bin/{{ .BinaryName }} ./main.go
./bin/{{ .BinaryName }} version
```

## Commands

- `version`: print build and version info
{{- if not .Minimal }}
- `greet --name World`: sample command for Cobra usage
- `mcp`: start MCP stdio server with a sample `hello` tool
{{- end }}

## Included Utilities

- `pkg/log`: zap logger wrapper with stdio/stderr/file output
- `utils/timeutil`: shared timestamp layout constants
- `utils/retry`: retry helpers with exponential backoff + jitter
- `utils/httputil`: HTTP client wrapper with retry and JSON helpers
- `utils/fileutil`: common file helpers (exists/copy/json read-write)

## Development

```bash
make help
make fmt
make test
make build
```

## License

MIT. See `LICENSE`.
